# 2026-08-17 15:00 — bugcheck 0x116 VIDEO_TDR_FAILURE (NEW class, NOT the device-lost/OOB class)

Timeline: n-max sweep arm 1 (n3 control, ORIGINAL weights, SPEED profile 320K,
VRAM 31,720/32,607). server.log ends 15:00:02 mid-task-1667 (shallow 700-tok
gen, checkpoint restored, NO cuda error / NO bounds-assert / NO cleanup line).
Windows: nvlddmkm error storm 15:00:37-41 (Id 153 ×~25, Id 14 ×2) → bugcheck
0x116 (VIDEO_TDR_FAILURE) param3=0xc000009a STATUS_INSUFFICIENT_RESOURCES →
BSOD; Kernel-Power 41 + EventLog 6008 at 17:01 reboot. MEMORY.DMP saved.

Classification: WDDM 2s-watchdog kill under VRAM-saturated CUDA (31.7 GB
resident + live desktop). Driver could not preempt/recover in TdrDelay=2s
(default; never raised — U1 item was pending). Workload at death was ordinary
decode; same class ran 8× clean minutes prior and for days in prod.

Signature comparison: 2026-08-16 GPU-lost incidents produced NO bugcheck
(device off bus, no BSOD) — proven OOB class, fixed+gated (GPULOST-BUGREPORT).
This is a DIFFERENT class. Only prior bugchecks on box: 2026-07-11 0x7e/0x3b,
2026-05 0x133/0x189 (unrelated).

Fix: TdrDelay=60 + TdrDdiDelay=60 (HKLM\SYSTEM\CurrentControlSet\Control\
GraphicsDrivers, DWORD) + reboot — staged as tdr-fix.reg. Interim mitigation:
sweep arms at -c 131072 (yarn flags byte-identical) → ~4.3 GB slack.

## ADDENDUM (same day, second incident + root-cause revision)

17:15 — GPU LOST during COLD LOAD (arm-1 relaunch, sweep wrapper -c 131072):
server died 20s in (weight-upload phase), last line "Starting GPU coredump
generation" (appears in NO healthy log = real GPU exception). nvlddmkm storm
17:15:09-11, adapter lost (no bugcheck; Windows survived, WSL NVML dead).
Coredump core_1787012111 (24KB, truncated): cuda-gdb reads "No CUDA kernels.
No CUDA contexts." — NO kernel in flight at exception time. Same binary+weights
loaded fine 14:45 and served 16h overnight. Driver 610.47 unchanged 3 months.

REVISED CHAIN: [unidentified GPU exception — device/driver level, source
unproven] × [CUDA_ENABLE_COREDUMP_ON_EXCEPTION=1, OUR env since Aug-15 ops
round: dump generation freezes a WDDM display GPU -> TDR storm -> BSOD or
adapter wedge] × [TdrDelay=2s default] = machine death. The coredump env is
the SEVERITY AMPLIFIER and is now REMOVED from all launchers (start-long-38,
start-max-38, start-long-38u, sweep wrapper). TdrDelay=60 staged (Desktop
tdr-fix.reg). Post-reboot protocol: idle-VRAM leak check, watched load +
short decode validation, soak before benchmarks. If the exception recurs
without the amplifier it should land as a LOGGED cuda error naming the op
(machine survives) -> real fingerprint. Era note (user's observation stands):
instability window == 3.8 era == near-cap VRAM (31.7/32.6) + amplifier env +
heavier kernels; headroom program queued.
